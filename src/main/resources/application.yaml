spring:
  application:
    name: security_example
  datasource:
    username: root
    password: 12345
    url: jdbc:mysql://localhost:3306/securitycodedecode
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    defer-datasource-initialization: true

